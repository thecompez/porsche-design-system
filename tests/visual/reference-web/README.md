# Official web reference baselines

These PNGs are rendered from `fixtures/button/reference.html` using the locked
`@porsche-design-system/components-js@4.4.0` loader. They are test-only
artifacts; no Porsche font, icon, or component runtime is shipped with the Qt
library.
